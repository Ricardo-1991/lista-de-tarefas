import { HeaderStyle, Container, TextTitle, } from "./style";
import {Feather} from "@expo/vector-icons";

export function FormCadastro() {
    return (
        <Container>
            <HeaderStyle>
           <TextTitle>Cadastro</TextTitle>
            </HeaderStyle>
        </Container>
    );
}